# secrets / local config
.env

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/

# airflow image build artifact: compose plugin staged from the host by
# `make airflow-image` (ADR-010 D1) — a ~65 MB binary, never commit it
airflow/docker-compose

# runtime data (generated, never committed)
file-drop/data/
logs/
airflow-logs/

# OS noise
.DS_Store

# local agent/tool artifacts (not part of the platform)
.mimosa/
.v2c/
out/
site/
push-public.sh

# local working docs + local verification records (kept on disk, not published)
STATE.md
BACKLOG.md
MASTER_PROMPT.md
devlogs/
SESSION_PROMPTS/
EVIDENCE/
docs/DESIGN_NOTES.md
