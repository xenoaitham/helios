# HELIOS dbt image (ADR-008 D5): pinned to the current stable 1.9.x line,
# adapter and core on matching minors. Exact versions recorded in
# EVIDENCE/phase-3-staging.md. No dbt packages — the project is hermetic.
dbt-core==1.9.11
dbt-postgres==1.9.1
# ADR-012 D1/D4: the OpenLineage dbt wrapper (active upstream, 1.53.0 released
# 2026-09-01; supports dbt-core>=1.0.0; emits ColumnLineageDatasetFacet from
# the openlineage_sql parse of the compiled SQL). openlineage-python is pinned
# explicitly so both emitters (this image, the airflow image) sit on known
# client versions; the exact dbt-core pin above stops pip from walking
# dbt-core upward behind the wrapper's back.
openlineage-dbt==1.53.0
openlineage-python==1.53.0
